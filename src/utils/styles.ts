import {css} from 'emotion';

export const formLayout = css({display: 'grid', gridRowGap: 16, marginBottom: 10, width: '100%'});

export const horizontalLayout = css({display: 'grid', gridAutoColumns: '1fr', gridAutoFlow: 'column', gridGap: 16});
